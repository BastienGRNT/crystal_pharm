import { complete } from '../../design';

// Ordre fixe : les sections supposent leur position (la bande d'en-tête est
// pleine largeur et sans marge haute).
export default complete({
	name: 'Classique',
	reorderable: false
});
